"""
Utilities Module

Common utility functions for formatting and exporting data.
"""

import pandas as pd


def format_table(df: pd.DataFrame, max_rows: int = 20) -> str:
    """Format DataFrame as pretty table for terminal output"""
    pass


def export_results(df: pd.DataFrame, output_file: str, format: str = 'csv'):
    """Export DataFrame to CSV, JSON, or Parquet"""
    pass
